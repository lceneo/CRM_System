services:
  chernorusyapp:
    container_name: chernorusycontainer
    build: .
    image: chernorusyimage:v1
    ports:
      - 80:80
    expose:
      - 80
    networks:
      - internalnet

networks:
   internalnet:
     driver: bridge
