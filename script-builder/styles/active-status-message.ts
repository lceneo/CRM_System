import {prefix} from "../const";

export const activeStatusMessage = `
    .${prefix}-active-status-message {
        text-align: center;
        font-size: 13px;
        line-height: 2em;
        background-color: #b29b57;
        display: block;
        height: 2em;
        margin: 0;
        border-radius: 10px;
    }
`
