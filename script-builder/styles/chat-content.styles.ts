import {prefix} from "../const";

export const chatContentStyles = `
    .${prefix}-chat-content {
        height: 100%;
    }
    
    .${prefix}-chat-content::-webkit-scrollbar {
        display: none;
    }
`
