import { prefix } from "../const";

export const chatFooterStyles = `
	.${prefix}-chat-footer {
		display: flex;
		flex-flow: row;
		justify-content: space-between;
		align-items: center;
		padding: 5px;
	}
`
